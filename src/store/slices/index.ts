import home, { homeSlice, updateTitle, updateSubtitle, addDescription } from './home.slice';
import news, { addArticle, selectNewsArticles } from './news.slice';

export { home, homeSlice, updateTitle, updateSubtitle, addDescription, news, addArticle, selectNewsArticles };
